# -*- mode: snippet -*-
# name: test
# key: test
# --
func Test${1:Name}(t *testing.T) {
	want := 1
	val, err := $1()
	if err != nil {
		t.Fatalf("err during test: %v", err)
	}
	if val != want {
		t.Fatalf("val: %d. wanted %d", val, want)
	}
}