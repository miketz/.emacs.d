# -*- mode: snippet -*-
# name: checkbox embedded in a label
# key: cbLab
#
# when cb and label are separate, there is an unclickable space between them.
# embedding cb inside the label eliminates the unclickable space.
# --
<label>
  <input id="${1:id}" name="${2:sharedGroupName}" type="checkbox" value="${3:1}" ${4:checked}>
  ${5:label}
</label><br>