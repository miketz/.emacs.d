# -*- for loop -*-
# name: for
# key: for
# expand-env: ((yas-indent-line 'fixed) (yas-wrap-around-region 'nil))
# --
var $1 = $2,
    len = ${3:collection}.length;
for (${1:i} = ${2:0}; $1 < len; $1 += 1) {
    $0
}